# Copy this file to .env and set real values before any public deployment.
# .env is gitignored — never commit real secrets.
DEFAULT_API_KEY=dev-local-siem-key-2026
MASTER_API_KEY=master-key-change-in-prod